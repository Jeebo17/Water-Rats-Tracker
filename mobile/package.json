{
  "name": "water-rats-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "init": "npx cap init \"Water Rats\" com.example.waterrats --web-dir=../dist",
    "build:web": "npm --prefix .. run build",
    "copy": "npx cap copy",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8"
  }
}
